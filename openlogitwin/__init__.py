"""OpenLogiTwin — conveyor sorting cell digital twin.

This is a thin entry-point package: the runtime lives in the repo's source dirs
(`protocol-gateway/`, `plc/`, `simulation/`, `telemetry/`, `scripts/`). The package
provides one memorable front door — `python -m openlogitwin` — and a console script
(`openlogitwin`) when installed editable (`pip install -e .`).
"""
__version__ = "0.7.0"
